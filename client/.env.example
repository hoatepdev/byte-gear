SOCKET_URL=http://localhost:8000
API_BASE_URL=http://localhost:8000/api/v1

NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ENCRYPT_SECRET=your-encryption-secret-here

# CRITICAL SECURITY: JWT Secret for middleware verification
# Must match server JWT_SECRET_KEY exactly
# Used to verify JWT signatures server-side in Next.js middleware
# Generate with: openssl rand -hex 64
JWT_SECRET=your-jwt-secret-must-match-server-JWT_SECRET_KEY